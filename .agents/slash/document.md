---
description: Run the document skill to sync or audit documentation against code
---

Use the `document` skill for this request. Treat this slash command as a manual shortcut only: run in dry-run mode unless the user explicitly passes `apply`, default sync mode to the working-tree diff when no change is specified, respect explicit staged diff, commit-range, and scope arguments when supplied, and report contract conflicts instead of editing them.
